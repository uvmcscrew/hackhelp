{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"builder": "RAILPACK",
		"buildCommand": "bun svelte-kit sync && bun --bun run build"
	},
	"deploy": {
		"startCommand": "realpath $(which bun) && echo $PORT && echo \"Starting\" && bun ./build/index.js echo \"Stopping\"",
		"preDeployCommand": "bun run db:migrate"
	}
}
