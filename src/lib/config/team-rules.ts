/**
 * Centralized team composition rules.
 *
 * Min/max members and per-role constraints are defined here so that
 * server validation and UI display always stay in sync.
 */

export const TEAM_MIN_SIZE = 4;
export const TEAM_MAX_SIZE = 6;

export const PROGRAMMERS_MIN = 3;
export const PROGRAMMERS_MAX = 4;

export const BUSINESS_MIN = 1;
export const BUSINESS_MAX = 2;
